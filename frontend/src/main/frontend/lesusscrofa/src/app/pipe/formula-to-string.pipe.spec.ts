import { FormulaToStringPipe } from './formula-to-string.pipe';

describe('FormulaToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FormulaToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
